# HueForge community libraries are hashed with normalized line endings.
# Drift between CRLF and LF breaks the SHA256 check on the subscriber side.
*.json text eol=lf
*.py text eol=lf
*.sh text eol=lf
